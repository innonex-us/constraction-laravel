APP_NAME="Construction Company Dev"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=4

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Docker MySQL Configuration (Development)
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=construction_dev
DB_USERNAME=construction_user
DB_PASSWORD=construction_password

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_DRIVER=log
CACHE_DRIVER=redis
CACHE_PREFIX=construction_dev
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync

# Redis Configuration
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# MailHog Configuration (for development)
MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="dev@construction.local"
MAIL_FROM_NAME="${APP_NAME}"

# File Upload Settings
UPLOAD_MAX_SIZE=10240
IMAGE_MAX_SIZE=5120

# Development Specific Settings
ASSET_URL=http://localhost:8000
VITE_DEV_SERVER_KEY=
VITE_DEV_SERVER_CERT=

# Filament Admin Panel
FILAMENT_DOMAIN=
FILAMENT_PATH=admin

# Security Settings (Development)
SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1
SESSION_SAME_SITE=lax

# Development Performance
CACHE_DRIVER=array
SESSION_DRIVER=file
QUEUE_CONNECTION=sync
